# Minimal dependencies to run the CLI pipeline demo
# CPU-only Torch build is sufficient
numpy>=1.20.0
pandas>=1.3.0
scipy>=1.7.0
scikit-learn>=1.0.0
biopython>=1.79

# Deep learning (only PyTorch used by code)
torch>=1.10.0; platform_system != 'Darwin'
# torchvision is not required for this code path

# Dimensionality reduction / clustering
umap-learn>=0.5.2
hdbscan>=0.8.28

# Visualization and utils
matplotlib>=3.5.0
seaborn>=0.11.2
plotly>=5.5.0

# CLI / logging
tqdm>=4.62.0
joblib>=1.1.0
click>=8.0.0
loguru>=0.6.0
